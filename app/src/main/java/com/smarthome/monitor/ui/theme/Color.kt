package com.smarthome.monitor.ui.theme

import androidx.compose.ui.graphics.Color

// ── Primary Brand ──
val SmartBlue = Color(0xFF4F8EF7)
val SmartBlueDark = Color(0xFF2563EB)
val SmartBlueLight = Color(0xFF93BBFC)

// ── Accent / Secondary ──
val TealAccent = Color(0xFF00D4AA)
val PurpleAccent = Color(0xFF8B5CF6)

// ── Backgrounds (Dark Theme) ──
val BackgroundDark = Color(0xFF050508)
val SurfaceDark = Color(0xFF141419)
val SurfaceVariantDark = Color(0xFF1A1A24)
val CardDark = Color(0x66141419) // 40% opacity for glass look

// ── Device State Colors ──
val StateOn = Color(0xFF22D3EE)        // Cyan
val StateOff = Color(0xFF475569)       // Slate gray
val StateError = Color(0xFFEF4444)     // Red
val StateDisconnected = Color(0xFFF59E0B) // Amber

// ── Device Type Accent Colors ──
val OutletColor = Color(0xFF38BDF8)    // Sky Blue
val SwitchColor = Color(0xFFC084FC)    // Neon Purple
val IronColor = Color(0xFFFB923C)      // Orange
val LightColor = Color(0xFFFDE047)     // Yellow
val CameraColor = Color(0xFF34D399)    // Emerald

// ── Safety Indicators ──
val SafetyRed = Color(0xFFFF3B3B)
val SafetyOrange = Color(0xFFFF8C00)
val SafetyGreen = Color(0xFF00E676)

// ── Text ──
val TextPrimary = Color(0xFFEDF2FF)
val TextSecondary = Color(0xFF94A3B8)
val TextMuted = Color(0xFF64748B)

// ── Borders & Dividers ──
val BorderSubtle = Color(0x14FFFFFF)
val GlassWhite = Color(0x14FFFFFF)
val GlassWhiteStrong = Color(0x28FFFFFF)
